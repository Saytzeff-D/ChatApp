import { PhoneNumValidatorDirective } from './phone-num-validator.directive';

describe('PhoneNumValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneNumValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
